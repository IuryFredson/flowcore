# Infrastructure

Local infrastructure is currently limited to SQL Server through `docker-compose.yml`.

Future additions can include API and web Dockerfiles after the MVP build is verified with the local SDK/toolchain.

